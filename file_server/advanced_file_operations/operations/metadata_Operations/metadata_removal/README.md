
# Metadata Removal Package

This package provides functionality for removing metadata from files, specifically images (EXIF data) and PDFs.

## Files

- `handler.go`: Defines the HTTP handlers for metadata removal requests.
- `processor.go`: Implements the core logic for metadata removal.
- `pdf`: Contains specific implementations for PDF metadata removal.
- `images`: Contains specific implementations for image metadata removal.
